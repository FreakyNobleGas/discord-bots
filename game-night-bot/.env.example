DISCORD_TOKEN=
DISCORD_GUILD_ID=          # For instant slash command sync during dev
RAWG_API_KEY=
ANTHROPIC_API_KEY=

ROTATION_CHANNEL_ID=       # Where /rotation embeds post
REMINDER_CHANNEL_ID=       # Where Thursday reminders post

MIN_SESSIONS=3             # Minimum sessions before a game can be voted out
EARLY_EXIT_VOTES=4         # Votes required for early exit
COOLDOWN_WEEKS=4           # Weeks before a game can re-enter after leaving
REMINDER_TIME=18:00        # Time (ET) to post Thursday reminder

DB_PATH=/app/data/rotation.db
